// import React from 'react';
// //import logements from '../logements.json';

// const Tags = ({ tags }) => (
//     <div className='tags-row'>
//       {tags.map((tag, index) => (
//         <span key={index} className='details__content--tag'>{tag}</span>
//       ))}
//     </div>
// );
// export default Tags;


// import React from 'react';

// const Tags = ({ tags }) => (
//     <div className='tags-row'>
//       {tags.map((tag, index) => (
//         <button key={index} className='details__content--tag'>{tag}</button>
//       ))}
//     </div>
// );

// export default Tags;

import React from 'react';

const Tags = () => {
  return (
    <div>
      
    </div>
  );
};

export default Tags;